# 닷친구 · DOT BUDDY — 서비스 설계서 v0.3

> 닷패드 체험단 · 어린이/초기 사용자를 위한 게이미피케이션 촉각 학습 서비스
> **v0.3 개정: P1·P2 실제 구현 + 자동 QA + 콘텐츠 파이프라인 검증 결과 전면 반영**
> 이 버전부터는 가설이 아니라 **실제로 돌려보고 확인한 사실**을 근거로 합니다.
> 작성일 2026-08-12

---

## 0. v0.2 → v0.3, 무엇이 달라졌나

v0.2까지는 DTMS 27페이지를 분석해 컨셉을 세운 기획 문서였습니다. 그 사이 **실제로 플레이어를 만들고, 헤드리스 브라우저로 27페이지 전체를 자동 순회시키고, 실기기 색 규약을 재사용하고, 마스코트를 배경 제거해 붙이고, 콘텐츠 한 건을 점자 데이터까지 완전히 갱신하는** 전 과정을 거쳤습니다. 그 과정에서 나온 사실들을 이번 버전에 편입합니다.

| 영역 | v0.2 상태 | v0.3 상태 |
|---|---|---|
| DTMS 플레이어 | 설계만 | **동작하는 단일 파일 앱** (`index.html`), 27페이지 전량 재생 검증 |
| interaction 스펙 | 제안 | **확정 스키마 + 27페이지 전량 데이터 작성 완료**, 사이드카 방식 채택 |
| 판정·재시도 로직 | 설계만 | **구현 + 자동 시뮬레이션 검증**(전 페이지 탈출 보장) |
| 음성 계층 | 설계만 | **구현 + 치명적 버그 발견·수정**(§5.1) |
| 비주얼 아이덴티티 | 미정 | **닷 디자인시스템 토큰 적용 + 마스코트 캐릭터 통합** |
| 접근성 대비 | 원칙만 서술 | **전수 계산 검증, 실패 항목 발견·수정** |
| 콘텐츠 편집 파이프라인 | 미정 | **liblouis ko-g1 파이프라인 역검증 완료**, 신뢰도 수준까지 문서화 |
| QA 방법론 | 없음 | **헤드리스 브라우저 자동 QA 체계 확립**(§8) |

가장 중요한 변화는 이겁니다: **"음성이 실패하면 앱 전체가 멈춘다"**는, 컨셉 단계에서는 절대 드러나지 않았을 치명적 결함을 실제로 발견하고 고쳤습니다. 이런 종류의 사실이 이 문서 전체의 신뢰도를 만듭니다.

---

## 1. 제품 개요 (유지)

**하루 5분, 닷패드 위에 사는 친구와 함께 손끝으로 세상을 읽는 법을 배우는 서비스.**

듀오링고의 뼈대(짧은 레슨 · 스트릭 · 수집 · 즉각 보상)를 가져오되, 모든 시각적 보상을 촉각과 소리로 번역했습니다. 하트(생명) 시스템은 의도적으로 배제합니다 — 틀림은 벌이 아니라 힌트가 나오는 순간이어야 합니다(§4.3).

캐릭터 페르소나는 **닷패드 자신**입니다. DTMS 원본이 이미 "안녕! 내 이름은 닷패드야"로 1인칭 자기소개를 하고 있어, 별도 마스코트를 새로 만들지 않고 이 페르소나를 계승·시각화했습니다(§6).

---

## 2. 시스템 아키텍처 (실제 구현 기준)

```
닷친구 (단일 페이지 앱, index.html)
├── 시작 화면(.gate)          자료 로딩 · 마스코트 히어로 · 오류 시 파일 직접 선택 폴백
├── DTMS 파서                items[] → 정규화된 page 배열, 600hex 그래픽 패딩 방어
├── interaction 엔진          사이드카(.iact.json) 병합 → 판정 → 재시도 → 보상
├── Voice 어댑터              TW_TTS 백엔드 + 타임아웃 안전장치(§5.1) — 슈퍼닷 교체 지점
├── 저장소                    localStorage, 실패 시 메모리 폴백
├── 렌더러
│   ├── 가상 닷패드 히어로     60×40 핀 · 여러 줄 점자 칸 / 한 줄 점자 칸(20셀) · 실물 버튼 배치
│   ├── 말풍선 + 마스코트      정적 캐릭터 이미지(§6)
│   ├── 코치 카드(부모용)      "지금 도와주기" · 정답 은닉 · 눌러야 할 버튼 하이라이트
│   └── 스티커 선반            실제 DTMS 촉각 자산에서 추출한 그림
└── 닷패드 SDK 계층            DotPadSDK 3.0.0, 한 브라우저 = 한 기기 연결
```

### 2.1 왜 이 구조인가

- **DTMS를 그대로 재생하는 플레이어 + 상호작용을 얹는 오버레이**로 나눈 것은 처음부터 의도한 설계입니다. 콘텐츠 저작(우상호 선생님, Tactile Studio)과 서비스 로직(닷친구 팀)의 책임을 분리해, 콘텐츠 저작자가 앱 코드를 몰라도 레슨을 늘릴 수 있게 합니다.
- **사이드카(`.iact.json`) 방식**을 택한 이유는 DTMS 스키마 소유권이 아직 정리되지 않았기 때문입니다(§9-1). `items[].interaction`으로 인라인 승격은 언제든 가능하고, 플레이어는 두 형태를 모두 읽습니다.
- **Voice를 어댑터 하나로 감싼 것**이 이번 라운드에서 가장 값을 했습니다. TW_TTS로 개발하다가 실제 버그(§5.1)를 발견했고, 백엔드 교체 지점이 명확했기 때문에 슈퍼닷 연동 시에도 상위 로직을 건드릴 필요가 없습니다.

---

## 3. 콘텐츠 파이프라인 — 실서비스에서 가장 중요한 발견

### 3.1 DTMS의 실제 구조 (역공학 완료)

`01_intro.dtms`(체험단 어린이 대상 닷패드 소개, 27페이지)를 전량 디코딩해 다음을 확인했습니다.

- 27페이지 중 **실제로 그림이 그려지는 곳은 8개뿐**(5·6·7·8 도형 시연, 20·21·22·23 4방 게임). 나머지 19개는 그래픽 영역(60×40)에 **내레이션을 여러 줄 점자로 그대로 표시**합니다.
- "네모"·"버튼" 같은 표현 중 상당수는 **화면 속 그림이 아니라 실제 기기 케이스·버튼을 만지라는 지시**입니다. 브라우저 시뮬레이터는 이 부분을 원천적으로 재현할 수 없습니다 — 코드로 고칠 문제가 아니라 콘텐츠 설계 자체가 "손에 쥔 실물"을 전제하기 때문입니다. 서비스 소개 자료나 부모 안내에 이 한계를 명시해야 합니다.
- 그래픽 자산 파일명(`2.dtm`, `4.dtm`, `26.dtm` 등)은 **전역 고유 식별자가 아닙니다.** 같은 이름이 서로 다른 페이지에서 서로 다른 그림을 가리키는 경우가 3건 확인됐습니다. 저작 도구가 세션 내에서 이름을 재사용하는 것으로 보입니다. **콘텐츠를 참조할 때는 반드시 `page` 번호를 키로 써야 하며, 파일명을 전역 자산 ID로 취급하면 안 됩니다.** (스티커 시스템에서 실제로 이 문제에 부딪혀 수정했습니다 — §6.2)

### 3.2 점자 데이터 재생성 파이프라인 (신규 확립)

콘텐츠를 수정하려면 평문뿐 아니라 그 평문이 만들어내는 점자 데이터(두 종류)도 함께 갱신해야 합니다. 이번에 **실제 파이프라인을 검증**했습니다.

```
새 평문 작성
   → liblouis(ko-g1.ctb)로 단어 단위 번역
   → 20셀 단위 줄바꿈(단어 경계 유지) → "한 줄 점자 칸"(text.data)
   → 20칸×6줄 격자 인코딩(칸당 가로 3픽셀·세로 6픽셀 간격) → "여러 줄 점자 칸"(graphic.data, 600hex)
```

**검증 방법**: 원본 DTMS의 실제 데이터를 역으로 디코딩해, 위 파이프라인의 재현 결과와 비트 단위로 대조했습니다.

| 채널 | 검증 결과 | 신뢰도 |
|---|---|---|
| 인코딩 테이블 확인 | 1페이지 원본 vs `ko-g1.ctb` 번역 결과 완전 일치 | 이 DTMS가 liblouis ko-g1로 제작됐음을 확정 |
| 한 줄 점자 칸(text.data) | 점자표시 12페이지 중 **10개 완전 일치** | 높음 — 신규 콘텐츠 제작에 신뢰하고 사용 |
| 여러 줄 점자 칸(graphic.data) | 점자표시 12페이지 중 **1개만 완전 일치** | **낮음** — 격자 좌표·인코딩 자체는 맞지만 원본 저작 도구의 정확한 줄바꿈 규칙(단어가 경계에 걸릴 때의 처리)을 재현하지 못함 |

**실서비스 함의**: 지금 이 파이프라인으로 그림이 없는 19개 페이지의 콘텐츠를 수정하면, **"한 줄 점자 칸"은 안심하고 배포**할 수 있지만 **"여러 줄 점자 칸"은 유효하고 정확한 점자이되 원본 도구와 줄바꿈 위치가 다를 수 있습니다.** 실제로 3페이지 콘텐츠 교체 시 이 방식대로 진행하고 사용자에게 이 한계를 명시했습니다.

**권장 조치**: 원본 저작 도구(Tactile Studio 또는 DTMS 익스포트 스크립트)의 그래픽 패널 줄바꿈 로직을 한 번 확보하면 이 격차가 없어집니다. 그 전까지는 **"한 줄 점자 칸" 우선 신뢰, "여러 줄 점자 칸"은 저작팀 재검증**을 콘텐츠 수정 워크플로 표준 절차로 둡니다.

### 3.3 콘텐츠 수정 표준 절차 (신규)

1. 평문(`text.plain`) 확정 — 물리적 형태·동작을 느끼게 하는 표현 우선(§3.1의 실물 전제 반영)
2. `text.data` 재생성 — §3.2 파이프라인, 자동 신뢰
3. `graphic.data` 재생성 — 같은 파이프라인, **저작팀 재검증 필수 표시**
4. interaction 사이드카 갱신 필요 시 `parentCoach`/`promptChild`도 같은 방향으로 동반 수정
5. 헤드리스 브라우저로 해당 페이지 렌더링 스크린샷 확인(§8)
6. DTMS 무결성 검사(그래픽 600hex 고정 길이, 페이지 수 불변) 후 배포

---

## 4. 상호작용 시스템 (확정 스펙)

### 4.1 interaction 스키마

```jsonc
{
  "type": "quadrant-choice | choice | count | yes-no | button-press | explore | none",
  "prompt": "부모/화면 표시용 질문",
  "promptChild": "아이에게 들려주는 음성 문구",
  "choices": [{ "key": "F1", "label": "...", "correct": true }],
  "onCorrect": { "say": "...", "reward": { "sparkle": 10, "sticker": "circle" } },
  "onWrong": { "say": "...", "retry": true, "maxRetry": 2, "hintAfter1": "...", "revealAfterMax": true },
  "parentCoach": "부모 코칭 문구 — 정답은 여기에도 노출 금지"
}
```

내부적으로 `yes-no`/`count`/`choice`/`quadrant-choice`는 전부 `choices[]`로, `button-press`는 `expect` 키 하나로 환원됩니다. 새 입력 방식을 만들지 않고 기존 F1~F4·패닝 키만 재사용합니다 — 예: "개수 세기"는 "n개 같으면 n번째 버튼"으로 매핑.

### 4.2 사이드카 vs 인라인

DTMS 스키마 소유권이 정리될 때까지 `xxx.dtms` 옆에 `xxx.iact.json`을 두는 방식을 표준으로 합니다. `page` 번호로 병합되며, 인라인 `items[].interaction`도 함께 지원해 이후 스펙이 공식 채택되면 마이그레이션 비용 없이 전환됩니다.

### 4.3 오답 처리 (검증 완료)

| 시도 | 동작 |
|---|---|
| 1회 오답 | 격려 + 같은 화면 재출력(재도전) |
| 2회 오답 | `hintAfter1` 힌트 |
| `maxRetry`(기본 2) 초과 | `revealAfterMax`면 정답 공개 후 진행, 아니면 격려 후 바로 진행 |

**전 페이지에서 "계속 틀려도 반드시 다음으로 넘어간다"를 자동 시뮬레이션으로 검증했습니다.** 예외는 실제로 눌러봐야 배우는 동작(버튼 누르기 게이트) 3곳뿐이며, 부모가 화면의 '다음' 버튼으로 우회할 수 있습니다. 아이를 절대 가두지 않는다는 원칙이 코드 수준에서 보장됩니다.

---

## 5. 신뢰성 요구사항 (신규 — QA에서 승격)

컨셉 문서였다면 이 항목은 없었을 겁니다. 실제로 자동 QA를 돌려서 발견한 결함을 정식 요구사항으로 못 박습니다.

### 5.1 ★ 음성 실패가 전체 흐름을 막아서는 안 된다 (치명적, 발견·수정 완료)

**증상**: 내레이션 음성 종료 → 질문 문구 음성 종료 → 그제서야 답변 버튼이 열리는 구조에서, 음성 재생 완료 콜백 하나에 전체 진행이 걸려 있었습니다. 서버 TTS가 실패하면 브라우저 자체 음성으로 폴백하는데, 음성 엔진이 응답 없는 환경(체험단 가정의 저사양 기기·오래된 브라우저·음성 미설치 환경에서 충분히 발생 가능)에서는 콜백이 영원히 안 옵니다. **자동 순회 테스트로 재현: 질문이 있는 16페이지 전부에서 답변 버튼이 하나도 안 열림.**

**요구사항**: 음성 재생은 **길이에 비례한 안전장치 타임아웃(최소 4초·최대 12초)** 을 반드시 둔다. TTS가 어떤 이유로든 응답하지 않아도 정해진 시간 안에 반드시 다음 단계로 진행한다. 정상 응답 시에는 지연을 추가하지 않는다.

**검증 기준**(단위 테스트로 확인 완료):
- TTS 완전 무응답 + 60자 텍스트 → 9000ms 이내 복구
- 최소 바닥값 4000ms, 최대 상한 12000ms
- 정상 응답 시 안전장치가 지연을 유발하지 않음(~50ms)

이 요구사항은 **슈퍼닷으로 백엔드를 교체해도 그대로 유지**되어야 합니다. 어떤 음성 엔진을 쓰든 "음성 실패 = 서비스 정지"가 되면 안 됩니다.

### 5.2 음성 재생 중임을 화면에도 표시한다

5.1의 안전장치 덕분에 최악의 경우 최대 12~20초 정도 기다려야 할 수 있는데, 그동안 화면에 아무 시각적 신호가 없으면 눈으로 보는 부모에게는 "멈춘 것"으로 보입니다. **캐릭터 옆에 "말하는 중" 표시(점 깜빡임)를 상시 둔다.** 이건 장식이 아니라 신뢰성 요구사항입니다 — 침묵하는 로딩 상태는 그 자체로 이탈 요인입니다.

### 5.3 대비 기준은 계산으로 검증하고 배포한다

디자인 토큰을 새로 적용하는 과정에서 `.pill` 기본 상태(액센트 텍스트 on 액센트 틴트 배경)가 4.25:1로 WCAG AA(4.5:1) 미달이었던 걸 실제 계산으로 발견했습니다. **육안으로는 발견되지 않는 종류의 결함**입니다.

**요구사항**: 새 색 토큰을 추가하거나 배색 조합을 바꿀 때는 육안 검수가 아니라 **명도 대비를 계산**해 4.5:1(본문)/3:1(UI 경계) 기준을 통과하는지 확인한 뒤 배포한다. §8의 자동 QA에 이 계산을 포함시킨다.

### 5.4 자산 참조는 안정적인 키를 쓴다

§3.1에서 발견한 대로 DTMS 그래픽 파일명은 전역 고유가 아닙니다. **콘텐츠를 재사용할 때는 `page` 번호(+필요시 잘라낼 영역 좌표)를 키로 쓰고, 파일명을 신뢰하지 않는다.**

---

## 6. 비주얼 아이덴티티

### 6.1 닷 디자인시스템 적용

임의 배색 대신 회사 표준 토큰으로 통일했습니다.

- 웜 크림 배경(`#FBF7EE`) · 잉크(`#211D16`) · 액센트(`#C43D00`, AA 대비 확인됨) · Pretendard
- **핀 on/off 색상은 dot-games-host의 실제 핀 프리뷰 코드에서 그대로 재사용**(on `#211d16`, off `#d9d3c6`) — 새로 만들지 않고 이미 검증된 값을 가져다 씀
- 아이콘은 Tabler 아이콘 계열로 통일(대부분 dot-games-host 기존 게임 코드에서 재사용)
- 보라·금색 네온 글로우 등 장식적 효과는 제거 — 실제 촉각 디스플레이는 빛나지 않으므로, 핀 표현은 "잉크가 살짝 그림자 지는" 절제된 방식을 씁니다

### 6.2 마스코트 캐릭터

주백준 님 제공 캐릭터(오렌지색 원형, 갈색 링, 손 흔드는 포즈)를 실제 자산으로 통합했습니다.

- 원본 PNG의 체크무늬는 투명이 아니라 그려진 패턴이었음 — `rembg`(U2Net)로 배경 제거
- 웹용 2사이즈로 최적화(히어로 480px/25KB, 아바타 160px/5KB)
- 적용 위치: 시작 화면 히어로(첫인상), 말풍선 아바타(상시 노출)
- 기존에 60×40 핀으로 추상적 표정(기본/기쁨/응원)을 그리던 로직은 폐기 — 이 캐릭터에 포즈 베리에이션이 없어 표정 전환이 불가능하기 때문. 정답/오답 피드백은 효과음·핀 패널 플래시·코치 카드 상태로 대체 전달

**미해결**: 포즈/표정 다양화. 이 계정에 이미 연결된 이미지 생성 도구로 같은 캐릭터 일관성을 유지하며 포즈를 늘릴 수 있는지 확인했으나, 실행 전 확인이 필요한 사안이라 보류 중입니다. 우선순위: 정답(기쁨) → 오답(응원) → 장기 미접속(잠) → 스트릭 달성(축하) 순으로 제안합니다.

---

## 7. 커리큘럼

| 유닛 | 이름 | 출처 | 상태 |
|---|---|---|---|
| **0** | 닷패드와 첫 만남 | DTMS 원본 27p + interaction | **완성** (P1·P2) |
| 1 | 모양 마을 | 신규 | 미착수 |
| 2 | 방 나누기 | 8p·20~23p 계승 | 미착수 |
| 3 | 버튼 친구들 | 11~17p 계승 | 미착수 |
| 4~9 | 좌표·점자·기억·회전·길찾기·그림읽기 | 기존 게임/신규 재활용 | 미착수 |

유닛 0이 이미 완성되어 있다는 게 가장 큰 이점입니다. 27페이지 그림책이 `interaction` 필드만으로 4개의 실제 게임(20~23p 4방 찾기)과 다수의 확인형 질문(yes-no·count·button-press)을 가진 완결된 레슨으로 바뀌었습니다.

---

## 8. QA 방법론 (신규 확립 — 운영 표준으로 채택 제안)

### 8.1 방법

`puppeteer-core` + `@sparticuz/chromium`(npm 레지스트리 경유 번들 바이너리) 조합으로, 외부 CDN 접근이 제한된 환경에서도 실제 헤드리스 Chromium을 확보해 다음을 자동 수행합니다.

1. **전 페이지 렌더링 순회** — 27페이지를 실제로 넘기며 콘솔 에러·에셋 404 감시
2. **인터랙션 시뮬레이션** — 정답 키를 실제로 입력해 판정·보상·스티커 적립까지 종단 검증
3. **명도 대비 계산** — 실제 계산된 RGB 값으로 WCAG 비율 산출, 육안 검수를 대체
4. **타임아웃 안전장치 단위 테스트** — 무응답 상황을 흉내 내 §5.1 요구사항이 지켜지는지 수치로 확인

### 8.2 왜 이게 필요한가

이번 라운드에서 코드 리뷰(정적 검토)만으로는 5.1의 치명적 결함을 발견하지 못했습니다. **"됐다고 말하기 전에 실제로 렌더링해서 확인한다"**를 이 프로젝트의 QA 원칙으로 삼습니다. 스크린샷 하나로 캐치되는 문제(배색)와, 실제 상호작용을 끝까지 시뮬레이션해야 캐치되는 문제(TTS 데드락)는 종류가 다르므로 둘 다 필요합니다.

### 8.3 배포 전 체크리스트 (제안)

- [ ] 27페이지(또는 해당 유닛 전 페이지) 콘솔 에러 0건
- [ ] 답변이 있는 모든 페이지에서 정답 입력 시 실제로 다음으로 진행되는지 확인
- [ ] 신규/변경된 색 조합 전부 대비 계산 통과
- [ ] TTS를 강제로 실패시켰을 때도 안전장치 시간 안에 복구되는지 확인
- [ ] 스크린샷 육안 확인(시작 화면·질문 화면·오답 화면·완료 화면 최소 4장)

---

## 9. 열린 사안 (갱신)

1. **DTMS 스키마 소유권** — `interaction` 필드를 공식 스펙으로 올릴지, 사이드카로 유지할지. (주백준 님 판단 필요)
2. **`graphic.data` 줄바꿈 로직** — §3.2. 원본 저작 도구의 정확한 규칙을 확보하면 콘텐츠 편집 신뢰도가 완전해집니다.
3. **슈퍼닷 연동** — 어댑터는 준비됐으나 실제 파라미터·라이선스(특히 사전 합성 파일 저장·배포 가능 여부) 미확인.
4. **마스코트 포즈 확장** — §6.2. 우선순위 목록에 대한 확인 필요.
5. **대상 연령·체험단 규모** — 문구 톤(7~9세 추정)과 세션 분량 확정에 필요.
6. **tw-app 편입 여부** — 별도 서비스로 갈지 `tactileworlds.com` 라우트로 들어갈지.

---

## 10. 로드맵 (갱신)

| Phase | 범위 | 상태 |
|---|---|---|
| **P1 · DTMS 플레이어** | 파싱·렌더링·닷패드 연동 | ✅ 완료 |
| **P2 · 상호작용** | 판정·보상·재시도, 사이드카 스펙 확정 | ✅ 완료 |
| **P2.5 · 비주얼/신뢰성** | 디자인시스템 적용, 마스코트 통합, TTS 안전장치, 대비 수정 | ✅ 완료 |
| **P3 · 콘텐츠 확장** | 유닛 1~9, `graphic.data` 줄바꿈 로직 확보, 스티커북 화면 | 다음 단계 |
| **P4 · 운영 준비** | 슈퍼닷 연동, 부모/교사 대시보드, 체험단 telemetry | 대기 |

**P1·P2가 컨셉이 아니라 실제로 동작하고 자동 검증까지 마친 상태**라는 게 이 버전의 핵심입니다. P3부터는 콘텐츠 볼륨을 늘리는 작업이고, 그 작업이 §3.3의 표준 절차를 따르면 이번에 겪은 시행착오를 반복하지 않습니다.
